type perfd, domain;
type perfd_exec, exec_type, file_type, vendor_file_type;
# Started by init
init_daemon_domain(perfd)

allow perfd system_data_file:dir w_dir_perms;
allow perfd system_data_file:sock_file { create unlink };
